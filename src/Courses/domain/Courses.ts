export interface Courses {
    name:string
    isCompleted: boolean
    userId: string
    createdAt: string
    updatedAt: string
}

export interface CoursesFront {
    id:string
    name:string
    isCompleted: boolean
    userId: string
    createdAt: string
    updatedAt: string
}
